import React from 'react';
import Login from "../components/login"; // Fix the import statement to use lowercase 'login'

function LoginPage() {
    return (
        <div className='py-8'>
            <Login />
        </div>
    )
}

export default LoginPage
